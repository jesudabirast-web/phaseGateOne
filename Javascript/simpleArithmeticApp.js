const prompt = require ('prompt-sync')();

let start date = date.now()

let firstNumber = (int(Math.random()*10);
let secondNumber = (int(Math.random()*10);
let total = firstNumber - secondNumber
console.log("enter result of first number - second number:  ");
let totalTime = startTime + endTime / 60

console.log(ProblemSolver(number));

public static int ProblemSolver(number) 

	if(userAnswer == total)
			console.log("correct answer");

	}else{
		(userAnswer != total)
			console.log("incorrect answer")
		





let stop date = date.now()

let result = (start date - stopdate) / -1000

console.log("your total is" + total)